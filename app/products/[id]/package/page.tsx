import { notFound, redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import { buildShopeeFinalPackage } from "@/lib/marketplaces/shopee/package";
import { CopyButton } from "./copy-button";
import { logout } from "@/app/actions";
const display=(value:any)=>typeof value==="number"?String(value).replace(".",","):String(value??"PENDENTE");
export default async function FinalPackagePage({params}:{params:Promise<{id:string}>}){const{id}=await params;const supabase=await createClient();const{data:claims}=await supabase.auth.getClaims();if(!claims?.claims?.sub)redirect("/login");const{data:product}=await supabase.from("products").select("*").eq("id",id).single();if(!product)notFound();const{data:listing}=await supabase.from("listings").select("*").eq("product_id",id).eq("marketplace","shopee").order("updated_at",{ascending:false}).limit(1).maybeSingle();if(!listing)redirect(`/products/${id}/review?error=${encodeURIComponent("Gere o anúncio sugerido antes de abrir a publicação")}`);const[{data:analysis},{data:pricing},{data:assets}]=await Promise.all([supabase.from("market_analyses").select("*").eq("product_id",id).eq("marketplace","shopee").order("analyzed_at",{ascending:false}).limit(1).maybeSingle(),supabase.from("pricing_scenarios").select("*").eq("product_id",id).eq("marketplace","shopee").eq("is_recommended",true).order("created_at",{ascending:false}).limit(1).maybeSingle(),supabase.from("product_assets").select("*").eq("product_id",id).eq("marketplace","shopee").order("created_at",{ascending:true})]);const pack=buildShopeeFinalPackage(product,listing,analysis,pricing,assets??[]),completePackage=pack.fields.map(item=>`[${item.section}] ${item.label} — ${item.status}:\n${display(item.value)}`).join("\n\n");return <div className="shell"><aside className="sidebar"><div className="brand">WJR <span>Seller IA</span></div><nav className="nav"><a href="/">Visão geral</a><a href="/studio/new">Seller Studio</a><a href="/products">Meus Produtos</a><a href="/settings/marketplaces">Configurações</a></nav><form action={logout} className="sidebarLogout"><button className="button" type="submit">Sair</button></form></aside><main className="main studioPage"><div className="crumbs"><a href={`/products/${id}`}>{product.name}</a><span>/</span><span>Pacote de publicação Shopee</span></div><div className="studioHeader"><div><div className="eyebrow">Anúncio pronto para publicar · SHOPEE</div><h1>Pacote de publicação Shopee</h1><p>Resultado final organizado para sua publicação manual: conteúdo, classificação, oferta, logística, mídia e validações em um só lugar.</p></div><div className="scoreBadge"><span>Prontidão</span><strong>{pack.ready?"100%":"—"}</strong><small>{pack.ready?"pronto para publicação":`${pack.pending.length} pendência(s)`}</small></div></div><section className="formSection packageSection"><div className="sectionTitle"><div><div className="eyebrow">Campos para publicação</div><h2>Anúncio pronto</h2></div><div className="packageActions"><span className={`statusChip ${pack.ready?"done":""}`}>{pack.ready?"Pronto para publicar":"Em preparação"}</span><CopyButton value={completePackage} label="Copiar pacote de publicação"/></div></div><div className="packageFields">{pack.fields.map(item=>{const text=display(item.value);return <div className="packageField" key={`${item.section}-${item.label}`}><div className="packageFieldHead"><span>{item.section} · {item.label}</span><CopyButton value={text}/></div><small className="note">{item.status}</small><div>{text}</div></div>})}</div></section><div className="workspaceGrid"><section className="formSection"><div className="eyebrow">Mídia</div><h2>Imagens e vídeo</h2><div className="facts"><div><span>Imagens</span><b>{pack.media.images.length}</b></div><div><span>Vídeos</span><b>{pack.media.videos.length}</b></div></div><p className="note">As imagens compõem a estratégia visual do anúncio. Vídeo de uso real é recomendado quando agregar demonstração, mas não bloqueia a publicação.</p></section><section className="formSection"><div className="eyebrow">Revisão final</div><h2>Pendências reais</h2>{pack.pending.length?<div className="pendingBox">{pack.pending.map(item=><p key={item}>• {item}</p>)}</div>:<p className="note">Nenhuma pendência estrutural detectada. Faça somente a conferência final antes de publicar.</p>}{pack.recommendations.length>0&&<div className="pendingBox"><b>Recomendações que não bloqueiam</b>{pack.recommendations.map(item=><p key={item}>• {item}</p>)}</div>}</section></div><div className="formActions"><a className="button" href={`/products/${id}`}>← Voltar ao anúncio</a><a className="button" href={`/products/${id}/readiness`}>Ver diagnóstico completo</a></div></main></div>}
