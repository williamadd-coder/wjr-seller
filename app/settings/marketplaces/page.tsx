import { redirect } from "next/navigation";import { createClient } from "@/lib/supabase/server";import { logout } from "@/app/actions";import { saveMarketplacePricingSettings } from "./actions";
export default async function MarketplaceSettings({searchParams}:{searchParams:Promise<{saved?:string;error?:string}>}){const query=await searchParams;const supabase=await createClient();const{data:claims}=await supabase.auth.getClaims();if(!claims?.claims?.sub)redirect("/login");const{data:settings}=await supabase.from("marketplace_pricing_settings").select("*").eq("marketplace","shopee").maybeSingle();return <div className="shell"><aside className="sidebar"><div className="brand">WJR <span>Seller IA</span></div><nav className="nav"><a href="/">Visão geral</a><a href="/studio/new">Seller Studio</a><a href="/products">Meus Produtos</a><a className="active" href="/settings/marketplaces">Configurações</a></nav><form action={logout} className="sidebarLogout"><button className="button">Sair</button></form></aside><main className="main studioPage"><div className="eyebrow">Configuração comercial</div><div className="studioHeader"><div><h1>Taxas e tarifas por marketplace</h1><p>Esses parâmetros alimentam as simulações de preço, lucro e margem. O Seller IA usa a configuração salva junto da pesquisa de concorrência; nenhuma taxa é tratada como oficial sem sua validação.</p></div></div>{query.saved&&<div className="authAlert">Configuração salva.</div>}{query.error&&<div className="authAlert error">{query.error}</div>}<section className="formSection"><div className="sectionTitle"><div><div className="eyebrow">Marketplace</div><h2>Shopee</h2></div><span className="statusChip done">Brasil</span></div><form action={saveMarketplacePricingSettings} className="pricingForm"><input type="hidden" name="marketplace" value="shopee"/><label>Comissão (%)<input name="commission_pct" inputMode="decimal" defaultValue={settings?.commission_pct??""} placeholder="Informe a taxa validada"/></label><label>Taxa fixa (R$)<input name="fixed_fee" inputMode="decimal" defaultValue={settings?.fixed_fee??""} placeholder="0,00"/></label><label>Impostos (%)<input name="taxes_pct" inputMode="decimal" defaultValue={settings?.taxes_pct??""} placeholder="0"/></label><label>Frete por venda (R$)<input name="freight_default" inputMode="decimal" defaultValue={settings?.freight_default??""} placeholder="0,00"/></label><label>Outros custos por venda (R$)<input name="other_costs" inputMode="decimal" defaultValue={settings?.other_costs??""} placeholder="0,00"/></label><button className="button primary" type="submit">Salvar configuração</button></form><p className="note">As tarifas podem variar por programa, categoria, campanha ou regra comercial. Esta tela é sua referência operacional e poderá ser versionada por marketplace nas próximas evoluções.</p></section></main></div>}
