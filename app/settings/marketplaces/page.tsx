import {redirect} from "next/navigation";import {createClient} from "@/lib/supabase/server";import {logout} from "@/app/actions";import {saveMarketplacePricingSettings} from "./actions";
export default async function MarketplaceSettings({searchParams}:{searchParams:Promise<{saved?:string;error?:string}>}){const query=await searchParams,supabase=await createClient();const{data:claims}=await supabase.auth.getClaims();if(!claims?.claims?.sub)redirect("/login");const{data:settings}=await supabase.from("marketplace_pricing_settings").select("*").eq("marketplace","shopee").maybeSingle();return <div className="shell"><aside className="sidebar"><div className="brand">WJR <span>Seller IA</span></div><nav className="nav"><a href="/">Visão geral</a><a href="/studio/new">Seller Studio</a><a href="/products">Meus Produtos</a><a className="active" href="/settings/marketplaces">Configurações</a></nav><form action={logout} className="sidebarLogout"><button className="button">Sair</button></form></aside><main className="main studioPage"><div className="eyebrow">Configuração comercial</div><div className="studioHeader"><div><h1>Shopee · taxas e custos</h1><p>A comissão Shopee é calculada automaticamente pela faixa do preço de venda. Você informa apenas seu perfil de vendedor e os custos particulares da sua operação.</p></div></div>{query.saved&&<div className="authAlert">Configuração salva.</div>}{query.error&&<div className="authAlert error">{query.error}</div>}<section className="formSection"><div className="sectionTitle"><div><div className="eyebrow">Política vigente desde 01/03/2026</div><h2>Comissão automática por faixa</h2></div><span className="statusChip done">Shopee Brasil</span></div><div className="facts"><div><span>Até R$ 79,99</span><b>20% + R$ 4</b></div><div><span>R$ 80 a R$ 99,99</span><b>14% + R$ 16</b></div><div><span>R$ 100 a R$ 199,99</span><b>14% + R$ 20</b></div><div><span>R$ 200 ou mais</span><b>14% + R$ 26</b></div></div><p className="note">Para CPF acima de 450 pedidos em 90 dias, o sistema acrescenta R$ 3 por item. O subsídio Pix não reduz o seu recebimento e não entra como custo. Para itens abaixo de R$ 12, a Shopee informa uma regra regressiva sem publicar uma fórmula completa; o Seller IA sinaliza essa faixa para conferência em vez de estimar.</p></section><section className="formSection"><form action={saveMarketplacePricingSettings} className="pricingForm"><input type="hidden" name="marketplace" value="shopee"/><label>Tipo de vendedor<select name="seller_type" defaultValue={settings?.seller_type??"cpf"}><option value="cpf">CPF</option><option value="cnpj">CNPJ</option></select></label><label>Pedidos nos últimos 90 dias<select name="cpf_over_450_orders_90d" defaultValue={settings?.cpf_over_450_orders_90d?"yes":"no"}><option value="no">Até 450 pedidos</option><option value="yes">Acima de 450 pedidos</option></select></label><label>Impostos da sua operação (%)<input name="taxes_pct" inputMode="decimal" defaultValue={settings?.taxes_pct??""} placeholder="0"/></label><label>Frete pago por você por venda (R$)<input name="freight_default" inputMode="decimal" defaultValue={settings?.freight_default??""} placeholder="0,00"/></label><label>Outros custos por venda (R$)<input name="other_costs" inputMode="decimal" defaultValue={settings?.other_costs??""} placeholder="0,00"/></label><button className="button primary" type="submit">Salvar configuração</button></form><p className="note">A taxa de transação já está contemplada na comissão Shopee. Impostos, frete e outros custos permanecem separados porque dependem da sua operação.</p></section></main></div>}
