export type MarketplaceFeeConfig={commissionPct:number;fixedFee:number;taxesPct:number;otherCosts:number};
export type PriceResult={salePrice:number;cost:number;commission:number;fixedFee:number;taxes:number;otherCosts:number;profit:number;marginPct:number};
export function calculateMarketplacePrice(salePrice:number,cost:number,config:MarketplaceFeeConfig):PriceResult{const commission=salePrice*config.commissionPct/100;const taxes=salePrice*config.taxesPct/100;const profit=salePrice-cost-commission-config.fixedFee-taxes-config.otherCosts;return{salePrice,cost,commission,fixedFee:config.fixedFee,taxes,otherCosts:config.otherCosts,profit,marginPct:salePrice>0?profit/salePrice*100:0};}
export function suggestCompetitivePrice(cost:number,config:MarketplaceFeeConfig,market:{min?:number|null;median?:number|null;max?:number|null},targetMarginPct=20){const variable=(config.commissionPct+config.taxesPct+targetMarginPct)/100;const floor=variable<1?(cost+config.fixedFee+config.otherCosts)/(1-variable):null;const candidates=[market.median,market.max,market.min].filter((v):v is number=>typeof v==="number"&&v>0);let suggested=candidates[0]??floor??null;if(suggested!=null&&floor!=null&&suggested<floor)suggested=floor;return{suggestedPrice:suggested,minimumSustainablePrice:floor,targetMarginPct,marketReference:candidates.length?"Faixa de concorrentes registrados":"Sem faixa de mercado registrada"};}
